/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/sutansite',
    assetPrefix: '/sutansite/',
    images: {
      unoptimized: true,
    },
  };
  
  export default nextConfig;
  